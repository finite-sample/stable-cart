# Act configuration for stable-cart project
# This file configures act to run GitHub Actions locally

# Use larger runner images that include more tools
-P ubuntu-latest=catthehacker/ubuntu:act-latest
-P ubuntu-20.04=catthehacker/ubuntu:act-20.04
-P ubuntu-18.04=catthehacker/ubuntu:act-18.04

# Set container architecture for Apple M-series compatibility
--container-architecture linux/amd64

# Use reusable containers to speed up subsequent runs
--reuse

# Environment variables for consistent behavior
--env PYTHONUNBUFFERED=1
--env PIP_DISABLE_PIP_VERSION_CHECK=1
--env PIP_NO_CACHE_DIR=0

# Use act's artifact server for uploads/downloads
--artifact-server-path /tmp/act-artifacts